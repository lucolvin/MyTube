# MyTube Environment Configuration

# Media Configuration
# Set this to your media folder path (absolute path recommended)
MEDIA_PATH=/path/to/your/media

# Database Configuration
DB_USER=mytube
DB_PASSWORD=mytube_password
DB_NAME=mytube

# JWT Secret - MUST be changed in production!
# Generate a secure random string, e.g.: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_IN_PRODUCTION

# Node Environment
NODE_ENV=production

DB_HOST_PORT=3403
DB_PORT=5432
REDIS_HOST_PORT=4815
REDIS_PORT=6379
BACKEND_HOST_PORT=7979
BACKEND_PORT=5199
FRONTEND_HOST_PORT=7594
FRONTEND_PORT=80
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
REACT_APP_API_URL=http://localhost:7979

# In your backend environment (e.g., .env or compose env vars)
CORS_ORIGIN=http://manx.emu-discus.ts.net:7594
API_URL=http://localhost:7979
